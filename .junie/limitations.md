# AI Limitations & Safety

## Restrictions
- **Directory Protection:** Do not modify the `recovered_blobs` directory without explicit instructions.
- **Verification:** Always verify build/compilation after non-trivial changes.
- **Context Awareness:** Always check `app/globals.css` before adding new color variables to ensure consistency with the existing theme.
